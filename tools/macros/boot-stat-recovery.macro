# Hold STAT across the first retail-boot key scan and capture the recovery path.
# Run at --normal-speed --reset with a full-address trace.
press STAT
wait 1s
release STAT
wait 2s
screenshot /tmp/boot-stat-recovery.png
